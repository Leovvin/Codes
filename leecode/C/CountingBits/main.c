#include <stdlib.h>
#include <stdio.h>
int* conuntBits(unsigned int num,int* returnSize);
int main(){

}
int* conuntBits(unsigned int num,int* returnSize){
    int* result=(int*)malloc(sizeof(int)*(*returnSize));
}
